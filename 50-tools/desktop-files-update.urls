# order matters - the first repo to have the translation wins
# and it's either a package or a repo of packages
# please note that these urls are only available internally - otherwise
# ichain would make this just too slow - api.opensuse.org is the external
# name for src-opensusesuse.de
https://api.opensuse.org/public/build/openSUSE:Leap:42.2/standard/x86_64
https://api.opensuse.org/public/build/openSUSE:Leap:42.2:NonFree/standard/x86_64
